git init // Create
git status // check the status of any new file

git add FileName // Add to localhost a file
git Add . // Add all

git commit -m "write comment here" // Commit
git push //push to the server



in case, if you haven't connected your account
git remote add origin https://github.com/manhtuanppa/TestGit.git
git log //checking connection

/////////////Branch
git branch // check all branch
git branch namebranch  //create a new branch
$ git branch -M branch_mane (main) // Reanme a branch
git checkout namebranch   // changes will be updated in new branch
git checkout -b namebranch   // switch to other branch
$ git branch -d branch_mane // remove a branch


git merge namebranch


///////////Advace
git rebase
git chery-pick

///////////Clone
$ git clone <url>
